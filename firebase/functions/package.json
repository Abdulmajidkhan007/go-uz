{
  "name": "vroom-firebase-functions",
  "version": "0.0.1",
  "private": true,
  "description": "Vroom Firebase Cloud Functions — status progression and background tasks",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
